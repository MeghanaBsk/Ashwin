pipeline {
    agent any
    
    stages {
        stage('Checkout') {
            steps {
                // Checkout the source code from Git
                git credentialsId: 'MeghanaBsk', url: 'https://github.com/MeghanaBsk/Ashwin.git'
            }
        }
        
        stage('Build') {
            steps {
                // Install Node.js dependencies
                sh 'npm install'
            }
        }
        
        stage('Test') {
            steps {
                // Run tests for the Node.js application
                sh 'npm test'
            }
        }
        
        stage('Deploy') {
            steps {
                // Deployment steps go here
                // Example: Deploy to a server or a cloud platform
                // sh 'npm run deploy'
            }
        }
    }
    
    post {
        success {
            // Post-build actions for success
            echo 'Build and test successful!'
        }
        failure {
            // Post-build actions for failure
            echo 'Build or test failed!'
        }
    }
}
